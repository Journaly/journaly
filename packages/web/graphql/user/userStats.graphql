query userStats($id: Int!) {
  userById(id: $id) {
    id
    name
    handle
    postsWrittenCount
    threadCommentsCount
    postCommentsCount
    thanksReceivedCount
    createdAt
    postActivity {
      count
      date
    }
  }
}
