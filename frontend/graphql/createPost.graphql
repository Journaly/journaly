mutation createPost(
  $title: String!
  $body: [EditorNode!]
  $languageId: Int!
  $status: PostStatus!
) {
  createPost(title: $title, body: $body, languageId: $languageId, status: $status) {
    id
  }
}
