# Migration `20200910205210-add-topic-translation-table`

This migration has been generated by Robin MacPherson at 9/10/2020, 8:52:10 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TYPE "UILanguage" AS ENUM ('ENGLISH', 'GERMAN');

CREATE TABLE "public"."TopicTranslation" (
"id" SERIAL,"name" text  NOT NULL ,"topicId" integer  NOT NULL ,"uiLanguage" "UILanguage" NOT NULL ,
    PRIMARY KEY ("id"))

ALTER TABLE "public"."Post" ALTER COLUMN "status" SET DEFAULT 'DRAFT';

ALTER TABLE "public"."Topic" DROP CONSTRAINT IF EXiSTS "Topic_userId_fkey",
DROP COLUMN "name",
DROP COLUMN "userId",
ADD COLUMN "devName" text  NOT NULL ;

ALTER TABLE "public"."User" ALTER COLUMN "userRole" SET DEFAULT 'FREE_USER';

ALTER TABLE "public"."_PostToTopic" DROP CONSTRAINT IF EXiSTS "_PostToTopic_A_fkey";

ALTER TABLE "public"."_PostToTopic" DROP CONSTRAINT IF EXiSTS "_PostToTopic_B_fkey";

CREATE UNIQUE INDEX "TopicTranslation.uiLanguage_topicId" ON "public"."TopicTranslation"("uiLanguage","topicId")

ALTER TABLE "public"."TopicTranslation" ADD FOREIGN KEY ("topicId")REFERENCES "public"."Topic"("id") ON DELETE CASCADE  ON UPDATE CASCADE

DROP TABLE "public"."_PostToTopic";
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200908095206-add-thanks-and-db-constraints..20200910205210-add-topic-translation-table
--- datamodel.dml
+++ datamodel.dml
@@ -1,7 +1,7 @@
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url      = env("DATABASE_URL")
 }
 generator prisma_client {
   provider = "prisma-client-js"
@@ -17,9 +17,8 @@
   bio                 String?
   posts               Post[]
   subscription        UserSubscription?
   socialMedia         SocialMedia[]
-  interests           Topic[]
   location            Location?            @relation(fields: [locationId], references: [id])
   locationId          Int?
   languagesNative     LanguageNative[]
   languagesLearning   LanguageLearning[]
@@ -89,9 +88,8 @@
   author       User          @relation(fields: [authorId], references: [id])
   authorId     Int
   readTime     Int           @default(1)
   likes        PostLike[]
-  topic        Topic[]       @relation(references: [id])
   language     Language      @relation(fields: [languageId], references: [id])
   languageId   Int
   longitude    Float?
   latitude     Float?
@@ -118,18 +116,26 @@
   User    User[]
 }
 model Topic {
-  id            Int            @default(autoincrement()) @id
-  name          String
-  posts         Post[]         @relation(references: [id])
-  user          User?          @relation(fields: [userId], references: [id])
-  userId        Int?
-  userInterests UserInterest[]
-  postTopics    PostTopic[]
-  prompt        Prompt[]
+  id                Int                @default(autoincrement()) @id
+  devName           String
+  userInterests     UserInterest[]
+  postTopics        PostTopic[]
+  prompt            Prompt[]
+  topicTranslations TopicTranslation[]
 }
+model TopicTranslation {
+  id         Int        @default(autoincrement()) @id
+  uiLanguage UILanguage
+  name       String
+  topic      Topic      @relation(fields: [topicId], references: [id])
+  topicId    Int
+
+  @@unique([uiLanguage, topicId])
+}
+
 model UserInterest {
   id        Int      @default(autoincrement()) @id
   user      User     @relation(fields: [userId], references: [id])
   userId    Int
@@ -286,4 +292,9 @@
   YOUTUBE
   INSTAGRAM
   LINKEDIN
 }
+
+enum UILanguage {
+  ENGLISH
+  GERMAN
+}
```


