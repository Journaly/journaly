mutation createPost(
  $title: String!
  $body: [EditorNode!]
  $languageId: Int!
  $status: PostStatus!
  $images: [Images!]
) {
  createPost(
    title: $title
    body: $body
    languageId: $languageId
    status: $status
    images: $images
  ) {
    id
  }
}
