query userStats($id: Int!) {
  userById(id: $id) {
    id
    name
    handle
    postsWrittenCount
    languagesPostedInCount
    threadCommentsCount
    postCommentsCount
    thanksReceivedCount
    createdAt
    activityGraphData {
      date
      postCount
      threadCommentCount
      postCommentCount
    }
  }
}
