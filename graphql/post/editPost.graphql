query editPost($id: Int!, $uiLanguage: UILanguage!) {
  postById(id: $id) {
    title
    bodySrc
    language {
      id
    }
    images {
      id
      largeSize
      smallSize
      imageRole
    }
  }
  topics {
    ...TopicFragment
  }
  currentUser {
    ...UserWithLanguagesFragment
  }
}
