query profilePage($userId: Int!) {
  userById(id: $userId) {
    ...UserWithLanguagesFragment
  }
  posts(authorId: $userId, status: PUBLISHED) {
    ...PostCardFragment
  }
  currentUser {
    ...UserWithLanguagesFragment
  }
}
