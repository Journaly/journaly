mutation createPost(
  $title: String!
  $body: [EditorNode!]
  $languageId: Int!
  $status: PostStatus!
  $images: [ImageInput!]
) {
  createPost(
    title: $title
    body: $body
    languageId: $languageId
    status: $status
    images: $images
  ) {
    id
  }
}
